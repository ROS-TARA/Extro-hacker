
    {
  "manifest_version": 3,
  "name": "My New Tab Extension",
  "version": "1.0",
  "description": "Replaces the default new tab with a custom page.",
  "chrome_url_overrides": {
    "newtab": "newindex.html"
  }
}
